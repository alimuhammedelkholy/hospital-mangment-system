# Database assets

`../HMS_Enterprise_SQL_Server_Schema.sql` is the SQL Server schema source of truth. Foundation work must not modify it. Put only additive, reviewed operational scripts in `scripts/`.
